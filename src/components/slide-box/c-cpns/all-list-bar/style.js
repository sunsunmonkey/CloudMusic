import styled from "styled-components";

export const AllListBarWrapper = styled.div`
   margin-left: 20vw;
   overflow: scroll;
   height: 90vh;
   
   &::-webkit-scrollbar {
   display: none;
   }
`